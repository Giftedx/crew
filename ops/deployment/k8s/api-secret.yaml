apiVersion: v1
kind: Secret
metadata:
  name: udi-secrets
type: Opaque
stringData:
  # Replace placeholders with real values (kubectl apply -f after editing)
  DISCORD_BOT_TOKEN: "REPLACE_ME"
  DISCORD_WEBHOOK: "REPLACE_ME"
  DISCORD_PRIVATE_WEBHOOK: "REPLACE_ME"
  OPENROUTER_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  EXA_API_KEY: "REPLACE_ME"
  PERPLEXITY_API_KEY: "REPLACE_ME"
  COHERE_API_KEY: "REPLACE_ME"
