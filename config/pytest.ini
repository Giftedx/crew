[pytest]
markers =
    integration: marks integration tests (deselect with '-m "not integration"')
    fullstack: requires full dependency import (discord, fastapi) – disables LIGHTWEIGHT_IMPORT guard
    asyncio: marks tests that use asyncio
filterwarnings =
    ignore:ENABLE_ANALYSIS_HTTP_RETRY is deprecated; use ENABLE_HTTP_RETRY instead.:DeprecationWarning:core.http_utils
