"""
DEPRECATED: performance_monitor_final.py

This file is an exact duplicate of performance_monitor.py and was deprecated
in Phase 7 (Performance Consolidation - October 2025).

DEPRECATION DATE: October 19, 2025
REMOVAL TARGET: After Phase 7 validation (Week 12)
DO NOT ADD NEW CODE TO THIS FILE.

=============================================================================
MIGRATION GUIDE
=============================================================================

This monitor was a duplicate created during refactoring. Use the original:

BEFORE (performance_monitor_final.py):
```python
from ultimate_discord_intelligence_bot.agent_training.performance_monitor_final import (
    AgentPerformanceMonitor
)

monitor = AgentPerformanceMonitor()
monitor.record_agent_interaction(...)
report = monitor.generate_performance_report("agent_name")
```

AFTER (Use canonical performance_monitor.py directly):
```python
from ultimate_discord_intelligence_bot.agent_training.performance_monitor import (
    AgentPerformanceMonitor
)

monitor = AgentPerformanceMonitor()
monitor.record_agent_interaction(...)
report = monitor.generate_performance_report("agent_name")
```

OR (Use unified AnalyticsService facade - RECOMMENDED):
```python
from ultimate_discord_intelligence_bot.observability import get_analytics_service

analytics = get_analytics_service()
analytics.record_agent_performance(
    agent_name="my_agent",
    task_type="analysis",
    quality_score=0.87,
    response_time=2.5,
    tools_used=["tool1", "tool2"]
)
result = analytics.get_agent_performance_report("my_agent", days=30)
if result.ok:
    print(f"Overall score: {result.data['overall_score']}")
```

=============================================================================
RATIONALE
=============================================================================

- performance_monitor_final.py is a 99% duplicate of performance_monitor.py
- Created during incomplete refactoring session (orphaned file)
- No unique functionality - all features exist in performance_monitor.py
- Causes confusion about which implementation to use
- Phase 7 consolidation eliminates unnecessary duplication

=============================================================================
AFFECTED COMPONENTS
=============================================================================

Known usages (to be migrated):
- None found (file appears unused in active code)

=============================================================================
REFERENCES
=============================================================================

- ADR-0005: Analytics Consolidation Strategy
- PHASE7_PERFORMANCE_CONSOLIDATION_COMPLETE.md
- Consolidation status: docs/architecture/consolidation-status.md

For questions, see Phase 7 documentation.
"""

raise ImportError(
    "performance_monitor_final.py is deprecated. "
    "Use agent_training/performance_monitor.py or AnalyticsService. "
    "See this file's docstring for migration guide."
)
